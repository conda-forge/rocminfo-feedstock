{% set name = "rocminfo" %}
{% set version = "3.0.0" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://github.com/RadeonOpenCompute/rocminfo/archive/roc-{{ version }}.tar.gz
  sha256: 2e3f9613461b1d8f335f135e3b2edb6b0ce5c9d3a3b2cfa9b7a3456273a73840

build:
  number: 0
  skip: True  # [not linux]

requirements:
  build:
    - {{ compiler('c') }}
    - {{ compiler('cxx') }}
    - cmake
    - make
    - {{ cdt('numactl-devel') }}
    - {{ cdt('pciutils-devel') }}
  host:
    - rocr-runtime {{ version }}
    - elfutils
  run:
    - rocr-runtime {{ version }}
    - {{ pin_compatible("elfutils", max_pin="x.x") }}

test:
  commands:
    # Can't run this command without an AMD GPU
    - test -f $PREFIX/bin/rocminfo

about:
  home: https://github.com/RadeonOpenCompute/rocminfo
  license: NCSA
  license_family: MIT
  license_file: License.txt
  summary: 'ROCm Application for Reporting System Info'

extra:
  recipe-maintainers:
    - isuruf
